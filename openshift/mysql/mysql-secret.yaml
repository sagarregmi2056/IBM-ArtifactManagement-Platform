apiVersion: v1
kind: Secret
metadata:
  name: mysql-root-secret    # ← Match CD pipeline name
type: Opaque
stringData:
  MYSQL_ROOT_PASSWORD: ${{ secrets.MYSQL_ROOT_PASSWORD }}  # ← Use secret
  MYSQL_DATABASE: spring_artifacts